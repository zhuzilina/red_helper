# API 配置说明

## 问题描述
当前应用已更新为使用OAuth认证方式，不再依赖过期的API Token。

## 解决方案

### 1. OAuth认证流程
应用现在使用OAuth 2.0认证方式，会自动处理token的获取和刷新：

1. **首次使用**：应用会自动打开浏览器进行授权
2. **Token管理**：自动保存和刷新访问令牌
3. **无需手动配置**：不需要手动更新API Token

### 2. 配置文件
只需要确保 `lib/env.dart` 文件中的配置正确：

```dart
const String clientId = "23082449719795571320238051220083.app.coze";
const String workflowId = "7536844432798629907";
// apiToken 不再需要，由OAuth自动管理
```

### 3. 验证配置
运行测试脚本验证API连接：

```bash
dart lib/pages/content_page/super_page/generate.dart
```

如果看到 "✅ 成功获取访问令牌" 说明OAuth认证成功。

## 当前状态
- ✅ 使用OAuth认证方式
- ✅ 自动token管理
- ✅ 应用会自动回退到模拟数据（如果认证失败）
- ✅ 用户界面会显示认证问题提示

## 认证流程
1. **首次启动**：应用会打开浏览器进行Coze授权
2. **授权成功**：自动保存访问令牌到本地存储
3. **Token过期**：自动使用刷新令牌获取新的访问令牌
4. **刷新失败**：重新进行完整授权流程

## 模拟数据
当OAuth认证失败时，应用会使用内置的模拟数据：
- 邓小平理论
- 毛泽东思想  
- 改革开放政策
- 其他主题的通用问题

## 调试信息
应用会在控制台输出详细的调试信息，包括：
- OAuth认证状态
- API请求状态
- 响应解析过程
- 错误详情

如果遇到问题，请查看控制台输出获取更多信息。

## 注意事项
- 首次使用需要网络连接进行OAuth授权
- 授权后token会保存在本地，离线时仍可使用
- 如果遇到认证问题，可以清除应用数据重新授权
